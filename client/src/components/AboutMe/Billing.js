import styled from 'styled-components';
import React from 'react';

const StyledBilling = styled.section``;

const Billing = ({ data }) => {
    return <StyledBilling>this is Billing</StyledBilling>;
};

export default Billing;
