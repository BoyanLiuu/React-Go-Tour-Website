import styled from 'styled-components';
import React from 'react';

const StyledBooking = styled.section``;

const Booking = ({ data }) => {
    return <StyledBooking>this is booking</StyledBooking>;
};

export default Booking;
