import styled from 'styled-components';
import React from 'react';

const StyledSetting = styled.section``;

const Setting = ({ data }) => {
    return <StyledSetting>this is Setting</StyledSetting>;
};

export default Setting;
