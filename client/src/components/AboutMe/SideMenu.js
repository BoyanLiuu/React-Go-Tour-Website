import styled from 'styled-components';
import React from 'react';

const StyledSideMenu = styled.section``;

const SideMenu = ({ data }) => {
    return <StyledSideMenu>this is SideMenu</StyledSideMenu>;
};

export default SideMenu;
